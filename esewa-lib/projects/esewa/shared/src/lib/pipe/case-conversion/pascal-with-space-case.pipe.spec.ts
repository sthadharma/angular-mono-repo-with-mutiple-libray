import { PascalWithSpaceCasePipe } from './pascal-with-space-case.pipe';

describe('PascalWithSpaceCasePipe', () => {
  it('create an instance', () => {
    const pipe = new PascalWithSpaceCasePipe();
    expect(pipe).toBeTruthy();
  });
});
